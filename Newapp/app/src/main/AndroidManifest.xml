<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.newapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TabLessActivity" />
        <activity android:name=".TabManyActivity" />
        <activity android:name=".login" />
        <activity android:name=".ClockActivity">
        </activity>
        <activity android:name=".LockMainActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/locktheme">
        </activity>
        <activity android:name=".wallSetting"></activity>
        <service android:name="com.example.dell.newapp.LockService">
            <intent-filter>
                <action android:name="com.example.dell.newapp.LockService"/>
            </intent-filter>
        </service>
        <service
            android:name=".notificationService"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".notificationReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="action.notification" />
            </intent-filter>
        </receiver>
    </application>
</manifest>